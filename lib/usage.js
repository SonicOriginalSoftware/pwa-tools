import pkg from "../package.json"

export const usage = `
  pwa-tools
  Author: ${pkg.author}
  Version: ${pkg.version}

  Usage:
    pwa-tools init              Initializes the current directory for the pwa-framework

  Options:

`
